Dockerfile based on Debian 12 (bookworm) that's configured to provide the
necessary system packages to properly build the project dependencies and should
produce binaries compatible with:

 * Debian 12+
 * Ubuntu 22.04+
 * Fedora 36+

*Note: Distributions of a comparable release may also work without issue but
have not been tested.*
